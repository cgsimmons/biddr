<%= @auction.errors.full_messages.join(', ') %>
<%= form_for @auction do |p| %>
  <div>
    <%= p.label :title%>
    <%= p.text_field :title %>
  </div>
  <div>
    <%= p.label :details%>
    <%= p.text_field :details %>
  </div>
  <div>
    <%= p.label :reserve_price%>
    <%= p.number_field :reserve_price %>
  </div>
  <div>
  <%= p.label :end_date%>
  <%= p.text_field :end_date %>
</div>
  <%= p.submit %>
  <% end %>
